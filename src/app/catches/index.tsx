import Page from "../../components/Page";
import PageReturnable from "../../components/PageReturnable";

export default function Catches() {
  return (
    <PageReturnable name="Catches">
      abc
    </PageReturnable>
  );
}
